# Generated by tinfoil 0.2.22. Do not edit by hand.
# Regenerate with: mix tinfoil.generate
name: Release

on:
  workflow_call:
    inputs:
      tag:
        description: "Tag to build and attach assets to. Defaults to the calling workflow's release event tag."
        required: false
        type: string
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag to build and attach assets to, e.g. v1.2.3"
        required: true
        type: string

permissions:
  contents: write
  id-token: write
  attestations: write

env:
  MIX_ENV: prod
  APP_NAME: hexpm_mcp

jobs:
  build:
    name: Build ${{ matrix.id }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - id: darwin
            targets: "darwin_arm64,darwin_x86_64"
            runner: macos-latest
          - id: linux
            targets: "linux_x86_64,linux_arm64"
            runner: ubuntu-latest
          - id: windows
            targets: "windows_x86_64"
            runner: ubuntu-latest
    runs-on: ${{ matrix.runner }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: "${{ inputs.tag || github.event.release.tag_name }}"

      - name: Set up Elixir/OTP
        uses: erlef/setup-beam@v1
        with:
          elixir-version: "1.19"
          otp-version: "28"

      - name: Set up Zig
        uses: mlugg/setup-zig@v2
        with:
          version: "0.15.2"

      - name: Cache mix deps and build
        uses: actions/cache@v5
        with:
          path: |
            deps
            _build
          key: ${{ runner.os }}-${{ matrix.id }}-mix-${{ hashFiles('mix.lock') }}
          restore-keys: |
            ${{ runner.os }}-${{ matrix.id }}-mix-

      - name: Install dependencies
        run: mix deps.get

      - name: Build and package release
        run: |
          for target in $(echo "${{ matrix.targets }}" | tr ',' ' '); do
            mix tinfoil.build --target "$target" --tag "${{ inputs.tag || github.event.release.tag_name }}"
          done

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: binary-${{ matrix.id }}
          path: _tinfoil/*
          if-no-files-found: error

      - name: Attest build provenance
        uses: actions/attest-build-provenance@v2
        with:
          subject-path: _tinfoil/*.tar.gz, _tinfoil/*.zip


  release:
    name: Release
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: "${{ inputs.tag || github.event.release.tag_name }}"

      - name: Set up Elixir/OTP
        uses: erlef/setup-beam@v1
        with:
          elixir-version: "1.19"
          otp-version: "28"

      - name: Download all artifacts
        uses: actions/download-artifact@v8
        with:
          path: artifacts
          merge-multiple: true

      - name: Cache mix deps and build
        uses: actions/cache@v5
        with:
          path: |
            deps
            _build
          key: ${{ runner.os }}-release-mix-${{ hashFiles('mix.lock') }}
          restore-keys: |
            ${{ runner.os }}-release-mix-

      - name: Install dependencies
        run: mix deps.get

      - name: Attach assets to GitHub Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: mix tinfoil.publish --input-dir artifacts --attach --tag "${{ inputs.tag || github.event.release.tag_name }}"

